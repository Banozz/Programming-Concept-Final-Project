#include "Entity.h"

/*
Entity::classEntity(struct Status , int classChoice){
	
	
}

void Entity::damage(struct status *stats, int dmg){
	*stats->hpCur -= dmg;
}

void Entity::manaRed(struct status *stats, int manaCost){
	//manaMax = manaCur;
	*stats->manaCur -= manaCost;
}

void Entity::drawEntity(sf::RenderWindow&){
	window.draw(window);	
} */
/*
int Entity::light(struct status *stats){// fungsi light attack
	int d = 0; srand(time(0));
    d = 18 + rand() % 20;
    d += *stats->dmg;
    return d;
}

int Entity::medium(struct status *stats){// fungsi medium attack
	int d = 0;
	srand(time(0));
    d = 45;
    d += *stats->dmg;
    return d;
}

int Entity::heavy(struct status *stats){// fungsi heavy attack
	int d = 0;
    srand(time(0));
    d = 45 + rand() % 25;
    d += *stats->dmg;
    return d;
} */
